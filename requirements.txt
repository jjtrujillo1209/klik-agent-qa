# Lead GenAI Orchestrator — Dependencias
# pip install -r requirements.txt

# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-dotenv>=1.0.0

# Anthropic Claude AI
anthropic>=0.34.0

# Scrapling — motor de scraping (reemplaza httpx + BeautifulSoup)
scrapling[all]>=0.4.8
curl_cffi>=0.7.0

# Excel/CSV export
openpyxl>=3.1.0

# Browser automation para LinkedIn auth
playwright>=1.40.0

# HTTP client (usado por scrappling_adapter)
httpx>=0.27.0
